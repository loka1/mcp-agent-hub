{
  "name": "mcp-agent-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-agent MCP hub and MCP client",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "dev": "npm run -w @mcp-hub/hub-server dev",
    "test": "npm run -ws test",
    "lint": "npm run -ws lint",
    "typecheck": "npm run -ws typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}

